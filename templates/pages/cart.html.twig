{% extends "layout.html.twig" %}

{% block title %}
    {{article.name}}
    {{ parent() }}
{% endblock %}

{% block head %}
{% endblock %}

{% block main %}
    <main id="content" class="cart">
        <header>
            <h2>Mon panier</h2>
            <p id="cart-total-price">Total : {{totalPrice}}€</p>
        </header>
        <main>
            <ul>
                {% for item in cart.items %}
                    <li>{{ item.name }}<a href="index.php?route=delete-article-cart">X</a></li>
                {% endfor %}
            </ul>
        </main>
        <footer>
            <a class="btn btn-cart-back" href="index.php">Continuer mes achats</a>
            <a class="btn btn-cart-order" href="index.php?route=pay">Finaliser la commande</a>
        </footer>
    </main>
{% endblock %}

{% block javascript %}
    {# <script type="module" src="assets/js/main-cart.js"></script> #}
{% endblock %}